
# Step 1 — FastAPI backend + Vanilla HTML/JS/CSS frontend

This is a fresh scaffold to start the RAG app. It includes:
- FastAPI backend
- Static frontend served from `/`
- Docker & docker-compose


## Run with Docker
```bash
docker compose up -d --build
# open http://localhost:8000
```
